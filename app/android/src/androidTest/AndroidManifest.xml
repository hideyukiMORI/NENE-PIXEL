<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Test APK only: real SAF grants, with storage restricted to acceptance fixtures. -->
        <provider
            android:name="io.github.hideyukimori.nenepixel.acceptance.AcceptanceDocumentsProvider"
            android:authorities="io.github.hideyukimori.nenepixel.test.acceptance.documents"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>
    </application>
</manifest>
